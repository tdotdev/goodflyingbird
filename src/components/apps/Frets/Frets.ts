


export type NoteMarker = {
  note: string
  color?: string
}

export type FretConfig = {
  scale: number[]
  title: string
  strings: number[]
  fretCount: number
  scaleLength: number
  scaleWidth: number
}